@{
    ViewData["Title"] = "Setare parolă nouă";
    var email = (ViewBag.Email as string) ?? "";
}

<div class="text-center" style="max-width:520px; margin: 40px auto;">
    <h2>Parolă nouă</h2>
    <p class="text-muted">Setează o parolă nouă pentru <b>@email</b>.</p>

    @if (ViewBag.Info != null)
    {
        <div class="alert alert-info">@ViewBag.Info</div>
    }

    @if (ViewBag.Error != null)
    {
        <div class="alert alert-danger">@ViewBag.Error</div>
    }

    <form method="post" asp-controller="Account" asp-action="ResetPassword">
        @Html.AntiForgeryToken()

        <div class="mb-3 text-start">
            <label class="form-label">Parolă nouă</label>
	            <div class="input-group">
	                <input id="NewPassword" class="form-control" type="password" name="newPassword" required autocomplete="new-password" />
	                <button class="btn btn-outline-secondary pw-eye-btn" type="button" data-target="NewPassword" aria-label="Arată parola">
	                    <span aria-hidden="true">👁</span>
	                </button>
	            </div>
            <div class="form-text">Minim 12 caractere, litere mari/mici, cifre și simbol.</div>
	            <div class="form-text">Ține apăsat pe 👁 ca să vezi parola.</div>
        </div>

        <div class="mb-3 text-start">
            <label class="form-label">Confirmă parola</label>
	            <div class="input-group">
	                <input id="ConfirmPassword" class="form-control" type="password" name="confirmPassword" required autocomplete="new-password" />
	                <button class="btn btn-outline-secondary pw-eye-btn" type="button" data-target="ConfirmPassword" aria-label="Arată parola">
	                    <span aria-hidden="true">👁</span>
	                </button>
	            </div>
	            <div class="form-text">Ține apăsat pe 👁 ca să vezi parola.</div>
        </div>

        <button class="btn btn-primary" type="submit">Salvează parola</button>
    </form>
</div>
