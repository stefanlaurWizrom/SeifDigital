@{
    ViewData["Title"] = "Autentificare";
    var email = (ViewBag.Email as string) ?? "";
}

<div class="text-center" style="max-width:520px; margin: 40px auto;">
    <h2>Autentificare</h2>
    <p>Introdu emailul @("@wizrom.ro") și parola.</p>

    @if (ViewBag.Mesaj != null)
    {
        <div class="alert alert-danger">@ViewBag.Mesaj</div>
    }

    @if (ViewBag.Info != null)
    {
        <div class="alert alert-info">@ViewBag.Info</div>
    }

    <form method="post" asp-controller="Account" asp-action="Login">
        @Html.AntiForgeryToken()

        <div class="mb-3 text-start">
            <label class="form-label">Email</label>
            <input class="form-control" type="email" name="email" value="@email" required />
            <div class="form-text">Doar adrese @("@wizrom.ro")</div>
        </div>

        <div class="mb-3 text-start">
            <label class="form-label">Parolă</label>

            <!-- Use Bootstrap-style input-group so the login field matches ResetPassword visuals -->
            <div class="input-group">
                <input id="loginPassword" class="form-control" type="password" name="password" required autocomplete="current-password" />
                <button class="btn btn-outline-secondary pw-eye-btn" type="button" data-target="loginPassword" aria-label="Arată parola">
                    <span aria-hidden="true">👁</span>
                </button>
            </div>

            <div class="form-text">
                Minim 12 caractere, litere mari/mici, cifre și simbol.
                <div class="small text-muted mt-1">Ține apăsat pe 👁 ca să vezi parola.</div>
            </div>
        </div>

        <div class="auth-page text-center">
            <button class="btn btn-primary mb-2"
                    type="submit"
                    name="mode"
                    value="login">
                Login
            </button>

            <div class="mt-2">
                <a class="text-decoration-underline" href="/Account/ForgotPassword">Am uitat parola</a>
            </div>

            <div class="alert alert-secondary mt-2">
                Dacă nu ai cont încă, apasă direct pe „Creează cont”, dupa ce ai completat mai sus email si parola.
            </div>

            <button class="btn btn-outline-dark mt-2"
                    type="submit"
                    name="mode"
                    value="create">
                Creează cont
            </button>
        </div>

    </form>
</div>
