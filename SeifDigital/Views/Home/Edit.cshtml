@model SeifDigital.Models.InformatieSensibilaEditViewModel
@{
    ViewData["Title"] = "Editează";
}

<h1 class="mt-2">Editează înregistrarea</h1>
<p class="text-muted">Modificările se salvează criptat (ca parola).</p>

<form asp-controller="Home" asp-action="Edit" method="post" class="mt-3">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="Id" />

    <div class="mb-3">
        <label class="form-label" asp-for="TitluAplicatie">Titlu</label>
        <input class="form-control" asp-for="TitluAplicatie" placeholder="Ex: RDP Server Contabilitate" />
        <span class="text-danger" asp-validation-for="TitluAplicatie"></span>
    </div>

    <div class="mb-3">
        <label class="form-label" asp-for="UsernameSalvat">User</label>
        <input class="form-control" asp-for="UsernameSalvat" placeholder="Ex: CONTAB\ion.popescu" />
        <span class="text-danger" asp-validation-for="UsernameSalvat"></span>
    </div>

    <div class="mb-3">
        <label class="form-label" asp-for="Parola">Parola</label>
        <div class="input-group">
            <input id="editPassword" class="form-control" name="Parola" value="@Model.Parola" type="password" placeholder="Parola..." autocomplete="new-password" />
            <button class="btn btn-outline-secondary" type="button" onclick="toggleEditPassword()" title="Arată / ascunde parola">👁</button>
        </div>
        <span class="text-danger" asp-validation-for="Parola"></span>
        <div class="form-text">Parola se salvează criptat.</div>
    </div>

    <div class="mb-3">
        <label class="form-label" asp-for="Detalii">Detalii</label>
        <textarea class="form-control" asp-for="Detalii" rows="4" placeholder="Ex: URL, port, observații, pași de conectare..."></textarea>
        <span class="text-danger" asp-validation-for="Detalii"></span>
        <div class="form-text">Detaliile se salvează criptat (ca parola).</div>
    </div>

    <div class="d-flex gap-2">
        <button type="submit" class="btn btn-primary">Salvează</button>
        <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary">Înapoi</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        function toggleEditPassword() {
            const el = document.getElementById('editPassword');
            if (!el) return;
            el.type = (el.type === 'password') ? 'text' : 'password';
        }
    </script>
}
